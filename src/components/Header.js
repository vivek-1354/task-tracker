import Button from "./Button";
const Header = () => {
  return (
    <header>
      <h1>Task Tracker</h1>
      <Button color="steelblue" fontSize="20px" />
    </header>
  );
};

export default Header;
